import 'package:flutter/material.dart';

class CamaraPage extends StatelessWidget {
  const CamaraPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Cámara'));
  }
}
